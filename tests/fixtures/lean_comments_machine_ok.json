{"tool_name":"Write","tool_input":{"file_path":"src/machine_ok.ts","content":"#!/usr/bin/env node\n// @ts-expect-error legacy vendor typings\n/* eslint-disable no-console */\nexport function authenticate(token: string): boolean {\n  return Boolean(token);\n}\nexport function refresh(token: string): string {\n  return token;\n}\nexport function revoke(token: string): void {\n  void token;\n}\n"}}
