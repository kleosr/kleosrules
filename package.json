{
  "name": "kleosrules",
  "version": "18.0.0",
  "private": true,
  "description": "Cursor harness pack — User Rules, companions, skills, Bash hooks, local NOW.md memory. No Rust. No MCP core dependency.",
  "license": "MIT",
  "scripts": {
    "doctor": "bash scripts/doctor.sh",
    "test": "bash tests/run.sh",
    "sync": "bash scripts/sync.sh",
    "install:macos": "bash MacOS/install.sh",
    "install:rules": "bash scripts/install.sh"
  }
}
