# USER RULES — how to paste

1. Open `user-rules/USER-RULES.paste.txt`
2. Cursor → Settings → Rules → User Rules
3. Replace the **entire** box with that file
4. New agent chat

That file is Option C **V9.2** (Native Lean + NO COMMENTS + Obedience Stack).

Do **not** also set `user-rules/option-c-core.mdc` to `alwaysApply: true`
(SINGLE SOURCE — User Rules already holds the body).

Then: `FORCE_SKILLS=1 bash install.sh`
