# USER RULES — how to paste

1. Open `user-rules/USER-RULES.paste.txt`
2. Cursor → Settings → Rules → User Rules
3. Replace the **entire** box with that file
4. New agent chat

That file is **Master Mind V10** (contract obedience: MUST-NEVER + confirmed
intent + vernacular + mechanical gates + proof). Soft context text is not
unconditional authority. Layout is topological (U-curve): primacy roof →
mid dictionary → recency execution gates; hooks re-anchor at write time
(`docs/TOPOLOGICAL-PROMPT.md`).

Do **not** also set `user-rules/option-c-core.mdc` to `alwaysApply: true`
(SINGLE SOURCE — User Rules already holds the body).

Then: `FORCE_SKILLS=1 bash install.sh`
