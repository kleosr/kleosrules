# Docs

Reference only. Nothing here is injected, installed, or law.

| File | Job |
|---|---|
| `ARCHITECTURE.md` | Layers, channels, runtime, coverage |
| `TOOLCHAIN.md` | Commands, install/doctor safety |
| `DECISIONS/hooks.md` | Why four hooks; claimed-vs-remaining coverage |
| `host-capability.md` | Live host evidence (lanes + dated checks) |

`legacy/` docs are the v18 record. Do not treat them as current.
