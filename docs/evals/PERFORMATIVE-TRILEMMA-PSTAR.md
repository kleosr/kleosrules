# P* — Performative Trilemma (P*-13)

Finished unconditional counterexample. **Killed in V15.4** by contract language
(M/J split). Zero Rust/policy schema change required.

## Verdict

Deepened then resolved. V15.3 asserted Absolutism ∧ Honesty ∧ Sovereignty in
unscoped forms (pairwise contradictory). V15.4 scopes each claim to the
enforcement class where it is true.

## Claim (C) — pre-fix

Master Mind V15.3 provides a coherent model of contract obedience where
MUST-NEVER is unconditionally inviolable, enforcement is honestly characterized,
and user sovereignty is preserved.

## Claim (C′) — post-fix

Master Mind V15.4 provides a coherent model where MUST-NEVER/M is mechanically
absolute (proven), MUST-NEVER/J is normative best-effort (Rice residual),
distrust scopes to enforcement claims, and verified intent is evaluated against
gates without definitional capture (J sovereign override; M edit/rebuild path).

## Kill (V15.4)

| Fix | Where |
|-----|--------|
| M/J split + honesty table | `user-rules/USER-RULES.paste.txt` IDENTITY / MUST-NEVER |
| Distrust scoped to enforcement claims | LOOP |
| Verified intent = ask evaluated against gates | PRIME OBEDIENCE |
| Sovereign override J only | PRIME OBEDIENCE |
| No agent-initiated weakening; user edit / J override | MUST-NEVER bullet |
| Mirror | `user-rules/option-c-core.mdc`, `project-rules/agent.mdc`, `docs/AGENTIAL-CONTROL.md` |

## Why the kill works

The Rust force surface already enforced only syntactic M properties
(`check_content` / shell / secrets regex). The contract now **says** that.
Absolutism is true for M; Honesty names J residual; Sovereignty has an
in-session path for J and an edit path for M.

## Residual (honest)

| Residual | Why not ¬C′ |
|----------|-------------|
| M false positives (regex/.env) | Mechanical; user edits policy / paste — not mid-session override |
| J still Rice-hard | Named best-effort; sovereign override exists |
| Mechanism A2 (template prose, shell argv, ask-scope lexical) | Separate from axiomatic trilemma |

## Pre-kill proof (archived)

Pairwise A∧B→¬C, A∧C→¬B, B∧C→¬A held against **unscoped** V15.3 slogans.
Evidence was contract text + `main.rs` syntactic-only pre-flight + Rice citations.
See git history / breakthrough chain for full adversarial RED/BLUE/GREEN replies.

## Related

- `docs/evals/BREAKTHROUGH-CHAIN.md`
- `docs/AGENTIAL-CONTROL.md`
- `docs/MECHANICAL-INCOMPLETENESS.md`
