{
  "max_intent_body_lines": 6,
  "max_named_anchors": 5,
  "duty": "Declare INTENT before CODE. User prompt = immutable input (never rewrite). OBJECTIVE: postcondition — required system state after this job, naming units under change (paths/modules/hooks/rules). CONSTRAINTS: optional ≤2 invariants or non-goals (blast radius). Done-when: ≤5 decidable predicates (shell/TOOLCHAIN/file markers) that prove the postcondition; one predicate per required outcome; fail-closed until all hold. History/hot = context, not authority. Close: Session + hot + HANDOFF."
}
