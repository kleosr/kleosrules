{
  "deny": [],
  "ask": [
    { "pattern": "rm\\s+(-[a-zA-Z]*r[a-zA-Z]*f|-[a-zA-Z]*f[a-zA-Z]*r|-[rf]{2})(?:\\s|$)", "message": "Confirm recursive rm (tree wipe)." },
    { "pattern": "find\\s+.*-delete(?:\\s|$)|rsync\\s+.*--delete", "message": "Confirm mass delete / sync-delete." }
  ],
  "opaque_write_ask_message": "",
  "opaque_write_deny_message": "Blocked shell write of app files. Use Cursor Write/StrReplace only — never tee/heredoc/redirect for CODE_EXT or CSS.",
  "prose_shell_deny_message": "Blocked prose comment in code write (Native Lean NO COMMENTS)."
}
