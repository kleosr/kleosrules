---
description: Auto-apply Native Lean / V10.1 contract gates — always on globally and in every synced repo
alwaysApply: true
---

# Native Lean — auto gates (always on)

These companions stay loaded pass what may: this file + `ponytail.mdc` +
`lean-code.mdc` + User Rule Master Mind V10.1. Do not drop them for speed or style.
Pass what may / never fight a deny / never Shell-bypass comment gates are not
soft craft defaults — agent “why” cannot override them. User may stop
ponytail/lean-code mode with an explicit stop phrase.

Before writing or editing application code:

1. Prefer **NO CODE** (reuse / delete / config) when that solves the ask.
2. **Silent** look for `.cursor/rules/vernacular.mdc`, `VERNACULAR.md`, `docs/VERNACULAR.md`. If found, Read and follow. If missing, private-match siblings only.
3. Always apply ponytail / lean-code mode (see `ponytail.mdc`). Read the ponytail skill for the full ladder. Invoke `/vernacular` when a contract exists or naming/layout is in play. Other skills on demand when the user asks.
4. **NO prose comments** — strip on touched path. Machine directives only for green build.
5. No monorepo / Nx / Turborepo / Clean Architecture theater unless already that shape or asked.
6. Zero-comment + secrets + vernacular + lean: `kleos-gate` on preToolUse /
   beforeShellExecution / read / MCP / Delete. PRE-FLIGHT `--check-content`
   before Write. Never fight a deny. Never use Shell to write app code with comments.

**Do not announce** missing vernacular / DEBT.md / TOOLCHAIN.md / AGENTS.md at chat open.

Full policy: User Rule **Master Mind V15.6+**.
Lean meter = finite size roofs — not ∀ semantic quality
(`docs/evals/LEAN-SIZE-QUALITY-PSTAR.md`).
