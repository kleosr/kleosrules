---
description: "Always-on ask-aware curator (inject vs declare)"
alwaysApply: true
---

# CONTEXT CURATOR

Harness injects `additional_context` only. Never rewrite the user prompt.
Declare `INTENT:` before CODE — OBJECTIVE maps ask → concrete outcome + paths/hooks/files; CONSTRAINTS = blast-radius negations only; `Done-when:` ≤5 disk/TOOLCHAIN anchors covering the full ask (ambitious, not a stub).
Before write: `vault_read` `wiki/hot.md` then `wiki/index.md`.
Before Done: Session LAYER CHECK (prompt|context|harness|loop|graph).
Ephemeral: `/state/` overwrite-only; cleared on stop success.
Law: `docs/ARCHITECTURE.md` + `docs/CURATOR.md`.
