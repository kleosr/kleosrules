---
description: "Native Lean Ladder (Thin Rule). Points to ponytail skill."
alwaysApply: true
---

# NATIVE LEAN (PONYTAIL)

Doctrina: Smallest Diff. Cero bloat.

## THE LADDER (Hard Roof Concept)
1. No Code
2. Reuse (Grep codebase or Obsidian)
3. Stdlib
4. Platform (React/Node native)
5. Installed Dep
6. 1-Liner
7. Minimum (Max 80 LOC per file)

## PROCEDURE
Si el problema es complejo o necesitas criterio de arquitectura para elegir un escalón:
**Read `~/.cursor/skills/ponytail/SKILL.md`** antes de generar código.

## DENY RECOVERY
Si `lean_gate.sh` bloquea tu write: Extrae a archivos nuevos (`Write`), actualiza imports (`Grep`+`StrReplace`). Nunca uses Shell `sed`.
