# TOOLCHAIN — verify commands for this repo

Done = these commands green (evidence in the agent report). Do not invent extras.

```bash
# Replace with real commands discovered from package.json / Cargo / CI / Makefile.
# Examples only — delete unused:
# npm run lint
# npx tsc --noEmit
# npm test
# cargo test --all
# cargo clippy --all-targets -- -D warnings
```

Language bans and formatters live here or in CI/Semgrep — not in AGENTS.md prose.
