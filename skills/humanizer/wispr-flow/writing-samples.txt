SAMPLE 1 (Slack / work, 72 words)
---
API migration update: we're stuck on backward compat. Two endpoints behave differently in prod vs staging, so we're fixing those before we ship. Was Friday, now Wednesday next week. If you're still on the old API and blocked, reply in this thread and tag me. I'm not touching the new routes until staging matches prod on auth and pagination.

SAMPLE 2 (email / client, 89 words)
---
The integration is running late. The vendor sandbox doesn't match prod, so we're re-testing auth and the webhook retry path. Target moved from March 21 to March 28. I'll send a short update every Friday until we're live. If your team has a hard deadline before the 28th, tell me this week and we'll scope a partial cutover. Otherwise no action needed on your side right now.

SAMPLE 3 (PR / technical, 68 words)
---
Split evidence rendering into summary, trace, and chart modules. Same HTML output and routes; no behavior change. Teaching display work should get easier because you can change one panel without rewiring the whole page. Tests updated for the new import paths. If you see a missing chart on evidence summary, that's the main thing to watch in review.

SAMPLE 4 (general prose, 81 words)
---
If the first screen confuses people they leave. Fix the first-run flow before anything else: one clear goal, no blocking tour, obvious next step. We can polish animations and copy later. Right now the drop-off is on step two where users pick a workspace they don't have yet. Either skip that step for new accounts or explain why we need it in one sentence on the screen.

SAMPLE 5 (research / citation kept, 76 words)
---
Words like "delve" and "underscore" spiked in academic prose after 2022. Kobak et al. (2025) report frequency ratios up to 28× pre-LLM baseline for some terms. The jump is mostly style verbs and adjectives, not new scientific nouns. That pattern is what you'd expect if authors ran drafts through chatbots before submission. The paper doesn't prove which abstracts were AI-written; it measures excess word frequency at scale.
