# AGENTS.md (rules adapter)

@../../AGENTS.md

Thin adapter pointing to the repository agent navigator at [`../../AGENTS.md`](../../AGENTS.md). Law is paste + `.mdc` + hooks.
Specific operational notes:
- User rules paste: `USER-RULES.paste.txt`.
- Rules installed globally to `~/.cursor/rules/`: `ponytail.mdc`, `agent.mdc`, `testing.mdc`, `vibe.mdc`, `postgres.mdc`, `next.mdc`, `vite.mdc`, `astro.mdc`, `complexity.mdc`, `pnpm.mdc`, `types.mdc`.
- Project layer: empty (`SHARED=()`).
