user-rules map. Parent ../AGENTS.md

Layers
- Paste USER-RULES.paste.txt = Prompt capsule (Cursor Settings → User Rules).
- agent.mdc = INTENT + harness doctrine (alwaysApply).
- ponytail.mdc + native-lean-autoload.mdc = Native Lean ladder + Cursor tools (Write/Shell/Read/Grep).
- vernacular.mdc = thin hard bans (zero comments, no any, named exports); depth in skills/vernacular.
- types.mdc / testing.mdc / debugging.mdc = scoped.

Steel vs steering
- Steering: these .mdc files + skills (model follows).
- Steel: shared/hooks + lean.json/intent.json (hooks deny/allow mechanically).

Done: paste USER-RULES then new chat; FORCE=1 bash shared/hooks/fleet_sync.sh install
Hard stops: no secrets in paste; no Rust claims; Cursor-native field names (additional_context, permission, continue)
