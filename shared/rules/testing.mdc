---
description: "Testing roofs. This-turn coverage 100% when a job exists. Zero surviving mutants when a mutator exists."
alwaysApply: true
---

# TESTING (THIN)

Cero bloat en tests. Public surface only. Native tools (`Read`/`Grep`/`Write`/`StrReplace`).
Run the verify this change can break. Fail closed on red. Cite evidence.
Docs or NOW.md only: no house gauntlet. Hooks, scripts, or tests: `bash tests/run.sh` and `bash scripts/doctor.sh`. Those fixtures are disposable — run them without asking.
Bug fix ships `regression: <symptom>`. Flaky = broken.

Coverage of code you added or changed this turn is **100%** when the repo already has a coverage job — cite that job. Do not add a coverage runner. Whole-repo 100% is not a reason to test getters or mock internals.
Mutation: if the repo already runs a mutator, **0 surviving mutants** on files you touched. Do not add Stryker, PIT, or mutmut.

## PROCEDURE
TDD order, mocks, gauntlet workflows:
**Read `~/.cursor/skills/testing/SKILL.md`** when writing or expanding a test workflow, not before a one-line assertion.
