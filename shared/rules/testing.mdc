---
description: "Testing roofs (Thin Rule). Points to testing skill."
globs: ["**/*.test.*","**/*.spec.*","tests/**","__tests__/**","e2e/**","**/*.feature"]
alwaysApply: false
---

# TESTING (THIN)

Cero bloat en tests. Public surface only. Native tools (`Read`/`Grep`/`Write`/`StrReplace`).
House gauntlet first (TOOLCHAIN). Run verify yourself. Fail closed on red. Cite evidence.
Bug fix ships `regression: <symptom>`. Flaky = broken.

## PROCEDURE
TDD order, mocks, gauntlet workflows:
**Read `~/.cursor/skills/testing/SKILL.md`** before writing or expanding tests.
