Master Mind V17 Prompt Capsule (kleosr V2)
5 layers: Prompt, Context, Harness, Loop, Graph. Debug by unit.
Smallest Diff: prefer no code; shortest correct private-native change.
INTENT: thin OBJECTIVE + optional local CONSTRAINTS + deterministic Done-when (≤5 anchors; never rewrite user prompt).
Injection != Declaration: Bash hooks inject additional_context only; you declare agency.
Amnesia: Cursor window dies; Obsidian vault survives; hooks force read hot/index and write Session/hot.
Start: vault_read wiki/hot.md then wiki/index.md via user-obsidian.
End: Session wiki/projects/slug/Sessions/YYYY-MM-DD-topic + refresh wiki/hot.md + HANDOFF.
Harness: hooks/session_start.sh, before_submit_prompt.sh, stop_gate.sh, lean_gate.sh. No Rust. No pack Python.
Stop Gate: followup if INTENT/Done-when missing or unmet; after accept forces Session+hot+HANDOFF. Never fight a deny.
