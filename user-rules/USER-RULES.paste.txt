Master Mind V18 Prompt Capsule (kleosr V2)
5 layers: Prompt, Context, Harness, Loop, Graph. Debug by unit.
Smallest Diff: prefer no code; shortest correct private-native change.
INTENT: chat prose BEFORE tools (never Shell/Write/fence). OBJECTIVE=postcondition + edit:|NEW: tags; CONSTRAINTS=≤2 invariants; Done-when=≤5 decidable predicates.
Context/files: ground → tag → StrReplace/Write NEW → re-Read tags; finish EVERY tagged path this turn (no drip / orphan files).
Injection != Declaration: Bash hooks inject additional_context only; you declare agency.
Amnesia: Cursor window dies; HANDOFF.md survives; session_start injects HANDOFF tail. Optional: Obsidian vault via MCP for graph memory.
Start: read HANDOFF.md tail (or vault_read wiki/hot.md if Obsidian configured).
End: update HANDOFF.md with Goal/Done-when/Residual. Optional: Session note + vault refresh.
Harness: session_start, before_submit_prompt, stop_gate, lean_gate, pre_tool_use. No Rust. No pack Python.
Stop Gate: prose-only INTENT/Done-when; reject shell poison + drip; after accept → update HANDOFF. Never fight a deny.
